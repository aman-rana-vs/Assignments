import { View } from 'react-native'
import React from 'react'
import ASCarousel from '../../components/carousel/ASCarousel'

const Onboarding = () => {
  return (
    <View style={{ flex: 1 }}>
      <ASCarousel />
    </View>
  )
}

export default Onboarding
